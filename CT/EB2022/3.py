def solution(grid):
    answer = 0
    
    for row in grid:
        for col in row:
            day_month = col.split()
            if len(day_month) > 4:
                pass
            else:
                pass
    


    # for i in range(len(grid)):
    #     subj1 = grid[i]
    #     for j in range(i+1, len(grid)):
    #         subj2 = grid[j]


    return answer